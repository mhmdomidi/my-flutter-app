enum AppScreen {
  splashScreen,
  whiteScreen,
  loginScreen,
  homeScreen,
  noNetworkScreen,
  somethingWentWrongScreen,
}

enum AppMainNavigatorPage {
  feedsPage,
  searchPage,
  activityPage,
  profilePage,
}

enum AppThemeMode {
  light,
  dark,
}

enum AppLogType {
  other,
  bloc,
  parser,
  navigation,
  appContentProvider,
  dependencyGraph,
  highPriorityDebug,
}

enum AppButtonProperties {
  standard,
  stretched,
  hollow,
  theme,
}

import 'package:flutter/material.dart';

abstract class FormEditorPage extends StatefulWidget {
  const FormEditorPage({Key? key}) : super(key: key);
}

abstract class EditProfilePage extends StatefulWidget {
  const EditProfilePage({Key? key}) : super(key: key);
}

abstract class EditProfilePicturePage extends StatefulWidget {
  const EditProfilePicturePage({Key? key}) : super(key: key);
}

abstract class EditPersonalInformationPage extends StatefulWidget {
  const EditPersonalInformationPage({Key? key}) : super(key: key);
}

abstract class SettingsPage extends StatefulWidget {
  const SettingsPage({Key? key}) : super(key: key);
}

abstract class SecurityPage extends StatefulWidget {
  const SecurityPage({Key? key}) : super(key: key);
}

abstract class PrivacyPage extends StatefulWidget {
  const PrivacyPage({Key? key}) : super(key: key);
}

abstract class NotificationsPage extends StatefulWidget {
  const NotificationsPage({Key? key}) : super(key: key);
}

abstract class BlockedAccountsPage extends StatefulWidget {
  const BlockedAccountsPage({Key? key}) : super(key: key);
}

abstract class RegistrationPage extends StatefulWidget {
  const RegistrationPage({Key? key}) : super(key: key);
}

abstract class RecoverAccountPage extends StatefulWidget {
  const RecoverAccountPage({Key? key}) : super(key: key);
}

abstract class ProfileRelationshipsPage extends StatefulWidget {
  const ProfileRelationshipsPage({Key? key}) : super(key: key);
}

abstract class CollectionsPage extends StatefulWidget {
  const CollectionsPage({Key? key}) : super(key: key);
}

abstract class CreateCollectionPage extends StatefulWidget {
  const CreateCollectionPage({Key? key}) : super(key: key);
}

abstract class CollectionPostsPage extends StatefulWidget {
  const CollectionPostsPage({Key? key}) : super(key: key);
}

abstract class CollectionPostsPageFull extends StatefulWidget {
  const CollectionPostsPageFull({Key? key}) : super(key: key);
}

abstract class ThemePage extends StatefulWidget {
  const ThemePage({Key? key}) : super(key: key);
}

abstract class FeedsPage extends StatefulWidget {
  const FeedsPage({Key? key}) : super(key: key);
}

abstract class CreatePostPage extends StatefulWidget {
  const CreatePostPage({Key? key}) : super(key: key);
}

abstract class PostlikesPage extends StatefulWidget {
  const PostlikesPage({Key? key}) : super(key: key);
}

abstract class PostcommentsPage extends StatefulWidget {
  const PostcommentsPage({Key? key}) : super(key: key);
}

abstract class PostCommentLikesPage extends StatefulWidget {
  const PostCommentLikesPage({Key? key}) : super(key: key);
}

abstract class ProfilePage extends StatefulWidget {
  const ProfilePage({Key? key}) : super(key: key);
}

abstract class PendingFollowRequestsPage extends StatefulWidget {
  const PendingFollowRequestsPage({Key? key}) : super(key: key);
}

abstract class PostPage extends StatefulWidget {
  const PostPage({Key? key}) : super(key: key);
}

abstract class ProfileUserPostsPage extends StatefulWidget {
  const ProfileUserPostsPage({Key? key}) : super(key: key);
}

abstract class SearchPostsPage extends StatefulWidget {
  const SearchPostsPage({Key? key}) : super(key: key);
}

abstract class ProfileUserTaggedInPostsPage extends StatefulWidget {
  const ProfileUserTaggedInPostsPage({Key? key}) : super(key: key);
}

abstract class HashtagPage extends StatefulWidget {
  const HashtagPage({Key? key}) : super(key: key);
}

abstract class SearchPage extends StatefulWidget {
  const SearchPage({Key? key}) : super(key: key);
}

abstract class HashtagPostsPage extends StatefulWidget {
  const HashtagPostsPage({Key? key}) : super(key: key);
}

abstract class ActivityPage extends StatefulWidget {
  const ActivityPage({Key? key}) : super(key: key);
}
